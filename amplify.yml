version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - cd admin
            - npm install
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: admin/dist
        files:
          - '**/*'
      cache:
        paths:
          - admin/node_modules/**/*
    appRoot: admin
